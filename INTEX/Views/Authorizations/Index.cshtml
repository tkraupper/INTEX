@model IEnumerable<INTEX.Models.Authorization>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AuthType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AuthType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AuthID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AuthID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AuthID })
        </td>
    </tr>
}

</table>
